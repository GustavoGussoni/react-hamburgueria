import styled from "styled-components";

export const TitleHeadThree = styled.h2`
  font-weight: 700;
  font-size: 1.125rem;
  line-height: 1.5rem;
  color: var(${(props) => props.color});
`;

export const TitleBody = styled.p`
  font-weight: 400;
  font-size: 0.875rem;
  line-height: 1.125rem;
  color: var(${(props) => props.color});
`;
