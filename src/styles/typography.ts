import styled from "styled-components";

export const TitleHeadThree = styled.h2`
  font-weight: 700;
  font-size: 1.125rem;
  line-height: 1.5rem;
  color: var(${(props) => props.color});
`;

export const TitleBody = styled.p`
  font-weight: 400;
  font-size: 0.875rem;
  line-height: 1.125rem;
  color: var(${(props) => props.color});
`;

export const FontHeadingOne = styled.h1`
  font-weight: 700;
  font-size: 26px;
  line-height: 34px;
  color: var(${(props) => props.color});
  font-family: "Inter", sans-serif;
`;

export const FontHeadingThree = styled.h2`
  font-weight: 700;
  font-size: 18px;
  line-height: 24px;
  color: var(${(props) => props.color});
  font-family: "Inter", sans-serif;
`;

export const FontHeadingFour = styled.h3`
  font-weight: 700;
  font-size: 14px;
  line-height: 24px;
  color: var(${(props) => props.color});
  font-family: "Inter", sans-serif;
`;

export const FontCapition = styled.span`
  font-weight: 400;
  font-size: 12px;
  line-height: 16px;
  color: var(${(props) => props.color});
  font-family: "Inter", sans-serif;
`;

export const FontBody = styled.p`
  font-weight: 400;
  font-size: 14px;
  line-height: 24px;
  color: var(${(props) => props.color});
  font-family: "Inter", sans-serif;
`;
